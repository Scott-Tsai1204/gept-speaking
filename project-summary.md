# 專案交接說明：英檢初級口說練習遊戲

給 Claude Code 的背景資料，延續先前在 Claude 聊天中的討論與已完成的原型。

## 專案目標

做一個練習「全民英檢初級」口說測驗的網頁遊戲。目標使用者是準備考初級口說的人，希望用遊戲化的方式增加練習動機，而不是單調的一問一答。

## 平台與環境限制

- **網頁優先**，但必須能在手機和平板的瀏覽器上順暢使用。
- **只需支援 Android**（不考慮 iOS / Safari），因為使用者只用 Android 裝置。以 Android 版 Chrome 為主要測試對象。
- 部署方式：**GitHub Pages**（靜態網站託管，免費、自動 HTTPS）。使用者已有 GitHub 帳號並成功上傳過一次。
- 因為 GitHub Pages 儲存庫是公開的，**任何 API 金鑰都不能放在前端程式碼裡**，未來如果要接後端 API（例如語音轉文字），需要另外一層後端代理（例如 Cloudflare Workers）來保管金鑰。

## 初級口說測驗的真實規則（設計依據）

- 共 18 題，作答時間約 10 分鐘，分三部分：
  1. **複誦**（5 題）：題目念兩次（間隔約 1-2 秒），聽完立即複誦，不印在紙上。
  2. **朗讀句子與短文**：先給 1 分鐘閱讀，再用約 1 分鐘朗讀出來。
  3. **回答問題**：暖身題、看法題、情境題，需先聽懂再作答，時限較短。
- 由兩位評分老師以 0-5 級分評分，平均後換算百分制；口說需 80 分才算通過。
- 官方最新題型與時限請以 LTTC 全民英檢官網（gept.org.tw）為準，測驗規格可能改版。

## 遊戲設計規劃

- **關卡對應題型**：複誦關、朗讀關、回答問題關，外加模擬完整考試的 Boss 關。
- **遊戲機制**：生命值、連擊（combo）、積分、每日任務、倒數計時製造壓力感；答錯的字進入「複習池」，之後以間隔重複的方式再次出現。
- **主題地圖**：以校園、餐廳、購物、交通等情境分區，逐步解鎖初級常用字彙。

## 技術評估紀錄

- **語音辨識**：因只需支援 Android，MVP 階段可直接使用瀏覽器內建的 `SpeechRecognition`（Web Speech API），不需要自架後端，開發最快。之後若準確度不足，可升級為呼叫外部語音轉文字 API。
- **語音辨識 API 選項比較**（用於未來升級）：
  - OpenAI（Whisper / gpt-4o-transcribe）：按分鐘計費（約每分鐘 0.003-0.006 美元），整合最簡單，目前是首選。
  - Google Cloud Speech-to-Text：有免費額度，可調參數較多，設定較複雜。
  - Azure AI Speech：有專門的「發音評測」功能（可評流利度、準確度等細項），適合之後做更精細的發音評分，但整合較複雜。
  - Deepgram / AssemblyAI：專做語音辨識的服務商，即時辨識評價不錯。
  - 目前傾向：先撐著用瀏覽器內建辨識，之後若要提升品質，優先評估 OpenAI。
- **回答問題關的評分**：辨識出文字後，交給 LLM 依「切題度、可理解度、字彙與語法」評分並給回饋，這部分需要後端代理（金鑰不能放前端）。
- **朗讀 / 複誦關的評分**：用辨識文字和標準文本做逐字比對（類似 WER，word error rate），算出對錯與相似度分數。
- **手機瀏覽器語音注意事項**：
  - 播放與錄音都需要「使用者先點擊」才能啟動（手機瀏覽器不允許自動播放/自動開麥克風）。
  - 麥克風只在 HTTPS 或 localhost 下可用。
  - Android 語音辨識需要網路連線（辨識送到 Google 伺服器處理）。
  - 系統提示音、靜音自動斷線等行為需要在程式裡處理（緩衝時間、重新啟動辨識）。

## 目前已完成的原型（隨附 index.html）

- 單一檔案、無建置流程，可直接上傳到 GitHub Pages 使用。
- 目前只做了**複誦關**：每輪 5 題（對應正式考試複誦題數），80 分算過關，播放題目兩次後自動開始錄音。
- 已有的功能：逐字比對打分、錯字標紅顯示、積分與連擊、簡單模式（作答前可先看句子）、辨識失敗時的錯誤提示、常錯字記錄在 localStorage、回合結束的總結畫面。
- 文字正規化處理了數字、日期序數、縮寫等，讓比對更準確。
- 響應式版面，考慮了手機安全區域（safe area）與觸控目標大小。

## 已知問題（使用者實測回報）

- **語音辨識準確度偏低**：瀏覽器內建辨識器對學習者的口音、停頓較不寬容，可能造成：
  - 句子說到一半被截斷（辨識器判定停頓就結束）。
  - 整句被誤判成完全不同的內容。
  - 同音字（如 there/their、to/two）被誤判為錯誤。
  - 系統提示音蓋掉開頭幾個字。
- 尚未確認是哪一種情況最主要，需要進一步除錯（例如在畫面上顯示辨識信心值、候選結果，或改成持續辨識、拉長停頓才結束的邏輯）。

## 下一步方向（依優先順序）

1. **先解決語音辨識準確度問題**：可能的修法包含改用持續辨識搭配自訂靜音判斷時間、採用辨識器回傳的多個候選答案取最佳匹配、加入同音字對照表、拉長錄音開始前的緩衝。
2. 視情況評估是否要換成外部語音轉文字 API（見上方比較）。
3. 加入朗讀關（1 分鐘閱讀倒數 + 朗讀計時 + 逐字比對）。
4. 加入遊戲層（生命、連擊視覺化、複習池、主題地圖）。
5. 加入回答問題關（需要後端代理 + LLM 評分）。
6. 使用者希望互動更豐富、更有遊戲感，不只是單調的文字問答（可考慮更多動畫、音效，若要做可移動的地圖或角色動作，可評估使用 Canvas 或 Phaser 這類遊戲引擎；目前的選單、結果卡片等畫面用一般 HTML/CSS 已經足夠）。

## 部署方式提醒

- 更新 `index.html` 後直接上傳（或用 git push）到 GitHub 儲存庫的 `main` 分支即可自動更新網站（GitHub Pages 設定為 Deploy from branch: main / root）。
- 若之後用 Claude Code 自動 commit / push，建議推到獨立分支（例如 dev）先行確認沒問題，再合併到 main，避免壞掉的版本直接上線（因為 main 分支會自動發布）。
- 若加入後端或 API 金鑰，切記加入 .gitignore，不可 commit 進公開儲存庫。
